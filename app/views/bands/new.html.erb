<h1>New band</h1>
<% if @band.errors.full_messages %>
  <% @band.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>
<%= render 'form' %>

<%= link_to 'Back', bands_path %>
