<form action="<%= track_notes_url(track) %>" method="POST">

  <input
    type="hidden"
    name="authenticity_token"
    value="<%= form_authenticity_token %>"
  >

  <label for="notes_text">
<textarea name="notes[text]" id="notes_text"></textarea>

  <input type="submit">

</form>